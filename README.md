# eac-kernel
Simple kernel research project for x64 Windows. 

### Building
Run `build.bat` from an administrative developer terminal.

### Usage
Run the resulting binary with a system module as a parameter.

### Disclaimer
Authorized research only. Modifying kernel structures is inherently unstable. 
